/** @type {{compile: (source:string)=>(string|{name: string, cause: { message:string, name: string, stack: string}, stack: string, message: string }), user: {}}
 */
const cljs = window.cljs
export default cljs;
